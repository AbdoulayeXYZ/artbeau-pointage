# Frontend Environment Variables for Production
# These MUST be prefixed with VITE_ to be available in the browser

VITE_API_URL=https://pointage.artbeaurescence.sn
VITE_SOCKET_URL=https://pointage.artbeaurescence.sn
VITE_APP_NAME=Art'Beau-Pointage
VITE_APP_VERSION=1.0.0

# Backend Configuration (for reference - not used by frontend)
NODE_ENV=production
PORT=3001

# JWT Configuration (GÉNÉREZ UNE CLÉ SÉCURISÉE)
# Utilisez: openssl rand -base64 32
JWT_SECRET=hbccVgsrngY/NxvJvGJhmRE1jvB3ZZ7aRJt5lop/a54=

# CORS Configuration - Mise à jour pour le domaine
CORS_ORIGIN=https://pointage.artbeaurescence.sn,http://pointage.artbeaurescence.sn

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=1000

# Database (SQLite)
DATABASE_PATH=/app/data/artbeau.db

# Optional: PostgreSQL si migration
# POSTGRES_USER=artbeau_user
# POSTGRES_PASSWORD=your-secure-password
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DB=artbeau_pointage

# Logs
LOG_LEVEL=info
LOG_FILE_PATH=/app/logs/artbeau.log

# Email (si vous ajoutez des notifications)
# SMTP_HOST=your-smtp-host
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password

# Backup (optionnel)
# BACKUP_ENABLED=true
# BACKUP_SCHEDULE=0 2 * * *  # Tous les jours à 2h du matin
